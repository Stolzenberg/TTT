@using System
@inherits PanelComponent
@namespace Mountain

<root>
    @if (Client.Local.DisplayName != Name)
    {
        <div class="card">
            <div class="name">@Name</div>
        </div>
    }
</root>

@code{
    [Property]
    public string Name { get; set; } = string.Empty;

    protected override int BuildHash()
    {
        return HashCode.Combine(Name);
    }
}